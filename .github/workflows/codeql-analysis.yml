name: "CodeQL"
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 0 * * 1'  # Optional: runs weekly

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write  # Required to create alerts in public repos

    strategy:
      fail-fast: false
      matrix:
        language: [ 'java' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v2
      with:
        languages: ${{ matrix.language }}

    # Explicit build step - replace with appropriate build commands
    - name: Build with Maven
      if: exists('pom.xml')
      run: mvn clean compile -Dmaven.test.skip=true

    - name: Build with Gradle
      if: exists('build.gradle')
      run: ./gradlew build -x test

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v2
